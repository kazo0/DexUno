namespace Dexter.Presentation;

/// <summary>The Pokédex's top plate (lens, indicator lights and the notched edge), shown above the shell and the detail pages.</summary>
public sealed partial class PokedexTopStrip : UserControl
{
    public PokedexTopStrip()
    {
        this.InitializeComponent();
    }
}
