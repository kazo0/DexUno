<Page x:Class="Dexter.Presentation.PokemonDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Dexter.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}"
      utu:StatusBar.Foreground="Light">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <local:PokedexTopStrip />

    <utu:NavigationBar Grid.Row="1" Content="{Binding Title}" Style="{StaticResource PrimaryNavigationBarStyle}">
      <utu:NavigationBar.MainCommand>
        <AppBarButton Label="Back" uen:Navigation.Request="-">
          <AppBarButton.Icon>
            <FontIcon Glyph="&#xE72B;" />
          </AppBarButton.Icon>
        </AppBarButton>
      </utu:NavigationBar.MainCommand>
    </utu:NavigationBar>

    <local:PokemonDetailView Grid.Row="2" />
  </Grid>
</Page>
